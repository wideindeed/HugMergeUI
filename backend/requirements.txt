fastapi
uvicorn[standard]
pytest
ruff
httpx
pydantic
pyyaml
huggingface_hub==1.25.1  # pin: an unpinned install of this silently dropped list_models()'s direction kwarg, broke search/browse in prod. app/hf/client.py is fixed for 1.25.1 - re-verify before bumping.
safetensors
numpy

# torch: install separately with the CUDA index, plain `pip install torch`
# silently grabs the CPU-only build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch
